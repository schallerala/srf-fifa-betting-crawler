node_modules/
dist/

# Bundled Apps Script output (regenerated by `nub run build`).
build/

# Locally cached HTML pages (see `nub run clean:cache`). Kept out of git so
# tests rely on committed fixtures, not transient captures.
cache/*.html

# Editor / tooling local state.
.history/
.claude/settings.local.json

# OS cruft.
.DS_Store
